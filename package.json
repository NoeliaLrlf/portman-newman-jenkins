{
	"name": "postman",
	"version": "1.0.0",
	"description": "Project for",
	"directories": {
		"test": "tests"
	},
	"scripts": {
		"api-tests-newRestApiV1": "newman run tests/DemoNewRestApiV1.postman_collection.json -e tests/OnPremise.postman_environment.json --reporters junit --reporter-junit-export newman.xml",
		"api-tests-oldRestApi": "newman run tests/DemoOldRestApi.postman_collection.json -e tests/OnPremise.postman_environment.json --reporters junit --reporter-junit-export newman.xml"

	},
	"author": "Noelia",
	"dependencies": {
		"newman": "^5.1.0"
	}
}